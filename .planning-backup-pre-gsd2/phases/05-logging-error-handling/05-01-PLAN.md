---
phase: 05-logging-error-handling
plan: 01
type: execute
wave: 2
depends_on:
  - 05-00
files_modified:
  - main.py
autonomous: true
requirements:
  - LOG-01
  - LOG-02
  - LOG-04

must_haves:
  truths:
    - "All relist actions are logged as structured JSON to logs/actions.jsonl"
    - "Errors and failures are logged with structured context (player, error message)"
    - "User can view recent action history via 'fifa-relist history' CLI subcommand"
    - "Human-readable app.log still works alongside JSON action log"
  artifacts:
    - path: "main.py"
      provides: "Enhanced setup_logging() with actions logger + history subcommand"
      contains: "setup_logging, actions logger, history subcommand"
    - path: "logs/actions.jsonl"
      provides: "Structured JSON action log file (created at runtime)"
  key_links:
    - from: "main.py setup_logging()"
      to: "logs/actions.jsonl"
      via: "JsonFormatter + FileHandler on 'actions' logger"
      pattern: "JsonFormatter|actions\\.jsonl"
    - from: "main.py build_parser()"
      to: "history subcommand"
      via: "argparse subparser 'history'"
      pattern: "history.*subparser|history.*add_parser"
    - from: "main.py main()"
      to: "action_logger"
      via: "logging.getLogger('actions') calls after relist actions"
      pattern: "action_logger\\.info"
---

<objective>
Wire structured JSON logging into main.py: add dedicated 'actions' logger with JsonFormatter writing to logs/actions.jsonl, and add 'history' CLI subcommand.

Purpose: Make all relist actions machine-parseable (LOG-01) with error context (LOG-02) and user-accessible history (LOG-04).
Output: Enhanced setup_logging(), history subcommand in CLI parser, action_logger calls at relist points.
</objective>

<execution_context>
@C:/Users/Ebby/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Ebby/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-logging-error-handling/05-RESEARCH.md

@main.py
@models/action_log.py
@browser/relist.py
</context>

<interfaces>
From models/action_log.py (created in Plan 00):
```python
class JsonFormatter(logging.Formatter):
    """Emit one JSON object per line with UTC ISO timestamp."""
    def format(self, record) -> str  # returns json.dumps(...)

def parse_action_history(path: Path, lines: int = 20) -> list[dict]:
    """Read last N lines of JSONL file, return list of dicts."""
```

From browser/relist.py (existing):
```python
class RelistExecutor:
    def relist_single(self, listing) -> RelistResult
    def relist_expired(self, expired_listings) -> RelistBatchResult
```

From models/relist_result.py (existing):
```python
@dataclass
class RelistResult:
    listing_index: int
    player_name: str
    old_price: int | None
    new_price: int | None
    success: bool
    error: str | None = None
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire JsonFormatter into setup_logging() with dedicated actions logger</name>
  <files>main.py</files>
  <action>
    Modify setup_logging() in main.py to add a third handler for structured action logging:

    1. Import JsonFormatter from models.action_log at the top of main.py
    2. In setup_logging(), after creating the existing file_handler and console_handler:
       - Create log_dir / "actions.jsonl" FileHandler (append mode, encoding="utf-8")
       - Set its level to INFO
       - Set its formatter to JsonFormatter()
       - Create a dedicated logger: actions_logger = logging.getLogger("actions")
       - actions_logger.setLevel(logging.INFO)
       - actions_logger.addHandler(actions_file_handler)
       - Set propagate=False so actions don't also appear in app.log
    3. The existing file_handler + console_handler remain unchanged (app.log still works)
    4. Add action logging calls in main() at these points:
       - After successful relist of a single player: action_logger.info("Rilist completato", extra={"action": "relist", "player_name": result.player_name, "success": True})
       - After failed relist: action_logger.warning("Rilist fallito", extra={"action": "relist", "player_name": result.player_name, "success": False, "error": result.error})
       - After batch completion: action_logger.info("Batch completato", extra={"action": "batch", "success": True})
       - Import: action_logger = logging.getLogger("actions") at module level

    Do NOT change the existing console output or app.log behavior.
    Use Italian log messages per project convention.
  </action>
  <verify>
    <automated>python -c "from main import setup_logging; setup_logging(); import logging; l = logging.getLogger('actions'); l.info('test', extra={'action': 'test', 'success': True}); print('OK')"</automated>
  </verify>
  <done>setup_logging() creates actions.jsonl handler, action_logger writes structured JSON at relist points</done>
</task>

<task type="auto">
  <name>Task 2: Add 'history' CLI subcommand</name>
  <files>main.py</files>
  <action>
    Add a 'history' subcommand to build_parser() and its handler in the __main__ block:

    1. In build_parser(), after the config subparser:
       - history_parser = sub.add_parser("history", help="View action history")
       - history_parser.add_argument("-n", "--lines", type=int, default=20, help="Number of entries to show")

    2. In the __main__ block, add elif args.command == "history": branch:
       - Import parse_action_history from models.action_log
       - Import Path
       - log_path = Path(__file__).parent / "logs" / "actions.jsonl"
       - Call parse_action_history(log_path, args.lines)
       - For each entry: print formatted line with timestamp, success indicator (✓/✗), message, and player_name if present
       - If file doesn't exist: print "Nessuna cronologia azioni trovata."
       - Format: [{timestamp}] {✓|✗} {message} - {player_name} (if present)
       - Use Italian messages per project convention
  </action>
  <verify>
    <automated>python -m pytest tests/test_action_log.py::test_history_parsing -x 2>/dev/null || python -c "from models.action_log import parse_action_history; from pathlib import Path; print('import OK')"</automated>
  </verify>
  <done>'fifa-relist history' shows last N action entries from actions.jsonl, handles missing file gracefully</done>
</task>

</tasks>

<verification>
1. setup_logging() creates 3 handlers (app.log, console, actions.jsonl) without conflicts
2. 'fifa-relist history' subcommand registered and callable
3. Action log entries written as valid JSONL after relist operations
4. Existing app.log and console output unchanged
5. Full test suite passes: pytest tests/ -v
</verification>

<success_criteria>
- logs/actions.jsonl receives structured JSON entries for relist actions
- 'fifa-relist history' displays recent entries with timestamp + success indicator
- app.log and console output unaffected
- All LOG-01, LOG-02, LOG-04 requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-logging-error-handling/05-01-SUMMARY.md`
</output>
