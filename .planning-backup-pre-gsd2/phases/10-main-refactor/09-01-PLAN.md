---
phase: 09-main-refactor
plan: 01
type: refactor
wave: 2
depends_on: [09-00-PLAN.md]
files_modified:
  - main.py
  - browser/session_keeper.py
  - logic/relist_engine.py
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Il main loop è descrittivo e compatto: delega le attese console, gli heartbeat e i controlli sessione al SessionKeeper e la logica condizionale del processing list al RelistEngine."
    - "Il RelistEngine elabora interamente i passaggi di _golden_retry_relist e relist_expired_listings."
  artifacts:
    - path: "browser/session_keeper.py"
      provides: "Classe per gestire le attese lunghe, la supervisione del bot_state e l'heartbeat browser"
      exports: ["SessionKeeper"]
    - path: "logic/relist_engine.py"
      provides: "Un componente superiore che racchiude logica di business e Playwright orchestration per la schermata trasferimenti"
      exports: ["RelistEngine"]
    - path: "main.py"
      provides: "Entrypoint applicazione ridotto a ~150 righe"
      exports: []
  key_links:
    - from: "main.py"
      to: "browser/session_keeper.py"
      via: "session_keeper.wait_with_heartbeat(next_wait)"
---

<objective>
Refactor Parte 2: Decostruzione finale di `main.py`. Estrarre la supervisione della sessione (Heartbeat, console mode logic, attese con interruzioni Telegram) all'interno di `SessionKeeper` ed estrarre la complessa catena if-else del mercato trasferimenti dentro `RelistEngine`. Il risultato finale deve essere un `main.py` pulito, documentato e altamente intelleggibile.

Purpose: Migliorare nettamente la leggibilità del monolite, rendendolo comprensibile a primo acchito ed esente dalla complessità procedurale pregressa.
Output: main.py compatto.
</objective>

<execution_context>
@C:/Users/Ebby/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Ebby/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Creazione SessionKeeper</name>
  <files>browser/session_keeper.py, main.py</files>
  <behavior>
    - Spostare `_active_wait_with_heartbeat` ed `ensure_session` da `browser/error_handler.py` e `main.py` al costruttore/istanza del `SessionKeeper`.
    - Spostare la logica iniziale del `while True:` che valuta `console_mode()` e `paused()` dentro a un metodo `.supervise_state()` del Keeper.
  </behavior>
  <action>
    Avviare `browser/session_keeper.py`. Cablare internamente e sostituire i riferimenti nel main.py gradualmente.
  </action>
</task>

<task type="auto">
  <name>Task 2: Creazione RelistEngine</name>
  <files>logic/relist_engine.py, main.py</files>
  <behavior>
    - Spostare `relist_expired_listings`, `_compute_next_wait`, `_cap_wait_to_golden_pre_nav`, e il gigantesco blocco che valuta la risposta del `detector.scan_listings()` dal main a questa Engine.
    - Questo engine avrà l'opportunità di chiamare le vecchie classi (`AuthManager`, `TransferMarketNavigator`, `RelistExecutor` [che è l'implementazione pratica Playwright]) iniettate da main o orchestrate localmente.
  </behavior>
  <action>
    Copiare minuziosamente le logiche if->else salvaguardandone ogni meccanica (doppio relist di verifica, processing count, hold window). 
  </action>
</task>

<task type="auto">
  <name>Task 3: Refactoring Finale main.py</name>
  <files>main.py</files>
  <behavior>
    - Il file finale non ha logiche di attesa dirette, se non la cabina di controllo. Deve importare tutti i moduli scritti nelle task precedenti o in `09-00` e disporli linearmente.
  </behavior>
</task>

</tasks>
