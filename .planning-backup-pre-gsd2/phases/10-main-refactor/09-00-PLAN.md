---
phase: 09-main-refactor
plan: 00
type: refactor
wave: 1
depends_on: []
files_modified:
  - main.py
  - config/log_config.py
  - logic/golden_hour.py
  - core/notification_batch.py
  - tests/test_golden_hour.py
  - tests/test_notification_batch.py
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Le costanti e la logica per le fasce orarie Golden Hour sono isolate e verificabili singolarmente tramite Unit Test, senza dipendenze da Playwright."
    - "Il sistema di rotazione dei log (CustomDailyRotatingHandler) viene inizializzato in un modulo configurazione separato."
    - "Il raggruppamento delle notifiche (Batching) per Telegram possiede un proprio modulo `NotificationBatch` per evitare le variabili sciolte nel loop del main."
  artifacts:
    - path: "logic/golden_hour.py"
      provides: "Metodi temporali e costanti spostate da main.py"
      exports: ["GOLDEN_HOURS", "get_next_golden_hour", "is_in_golden_period", "..."]
    - path: "config/log_config.py"
      provides: "Funzione `setup_logging` rimossa completamente da main"
      exports: ["setup_logging"]
    - path: "core/notification_batch.py"
      provides: "Classe per aggregare statistiche ed eseguire dispatch notifier"
      exports: ["NotificationBatch"]
  key_links:
    - from: "main.py"
      to: "logic/golden_hour.py"
      via: "Sostituzione dirette funzioni temporali"
      pattern: "golden_hour\\.is_in_golden_period\\(.*\\)"
    - from: "main.py"
      to: "core/notification_batch.py"
      via: "Istanza nel main.py usata nel loop continuo per inviare Telegram msg"
      pattern: "batch\\.add_success\\(.*\\); batch\\.flush_if_ready\\(.*\\)"
---

<objective>
Refactor Parte 1: Creare l'impalcatura dei nuovi moduli estraendo funzioni "pure" (Logica Golden Hour, Logging, Statistiche Notifiche) dal monolite `main.py`, scrivendo test TDD per i moduli appena creati prima di refactorizzare il main loop.

Purpose: Isolare il rumore nel `main.py` e abilitare Unit Test puri su logiche matematico/temporali critiche, proteggendole per il futuro.
Output: Tre nuovi moduli funzionanti e testati parallelamente a `main.py`.
</objective>

<execution_context>
@C:/Users/Ebby/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Ebby/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Estrazione Logica Golden Hour + TDD</name>
  <files>logic/golden_hour.py, tests/test_golden_hour.py</files>
  <behavior>
    - Spostare `GOLDEN_HOURS`, `GOLDEN_MINUTE`, ecc. in questo nuovo file come costanti.
    - Spostare `get_min_active_seconds`, `get_active_with_timer_count`, `get_next_golden_hour`, `is_in_golden_period`, `is_in_hold_window`, `is_close_to_golden`, `is_in_golden_window` da `main.py` a `logic/golden_hour.py`.
    - Scrivere `test_golden_hour.py` con test che congelano il tempo (tramite mock) e si assicurano del ritorno booleano atteso delle funzioni ai vari orari target (`15:00`, `16:09`, `16:11`, `18:20`).
  </behavior>
  <action>
    RED: Creare i file di test per coprire i comportamenti richiesti. Far fallire.
    GREEN: Incollare il codice logico intatto prelevato dal main attuale in `golden_hour.py`. Far passare i test.
    REFACTOR: Assicurare annotazioni di tipo, logging esternalizzato corretti.
  </action>
  <verify>
    <automated>pytest tests/test_golden_hour.py -x -v</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Estrazione Configurazione Logging</name>
  <files>config/log_config.py, main.py</files>
  <behavior>
    - Spostare `setup_logging` assieme alla classe `CustomDailyRotatingHandler` sviluppata precedentemente, localmente in `log_config.py`.
  </behavior>
  <action>
    Muovi `setup_logging` dal `main.py` verso `config/log_config.py`. Altera il `main.py` sostituendo con l'import:
    `from config.log_config import setup_logging`.
  </action>
</task>

<task type="auto">
  <name>Task 3: Estrazione Statistiche Notifiche + TDD</name>
  <files>core/notification_batch.py, tests/test_notification_batch.py</files>
  <behavior>
    - Spostare la logica delle `notification_accumulator` e l'invoke di `_send_batch_notification` all'interno di una classe wrapper dedicata.
    - Metodo `accumulate(scan, succeeded, failed, last_relist_error)`.
    - Metodo `is_ready_to_flush(bot_sleep_time, total_cycles) -> bool`.
    - Metodo `flush(app_config, page, logger)`.
  </behavior>
  <action>
    RED: Costruire casi d'uso dove `notify` accumula senza inviare finché `bot_sleep_time` < 120s e cicli < 5.
    GREEN: Creare `NotificationBatch` per assorbire queste regole.
    REFACTOR: Applicare al `main.py` alleggerendolo notevolmente sul fondo del while loop.
  </action>
</task>

</tasks>
