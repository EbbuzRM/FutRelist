---
phase: 05-logging-error-handling
plan: 00
type: tdd
wave: 1
depends_on: []
files_modified:
  - models/action_log.py
  - browser/rate_limiter.py
  - browser/error_handler.py
  - tests/test_action_log.py
  - tests/test_rate_limiter.py
  - tests/test_error_handler.py
  - requirements.txt
autonomous: true
requirements:
  - LOG-01
  - LOG-02
  - LOG-04
  - ERROR-01
  - ERROR-02
  - ERROR-03
  - ERROR-04

must_haves:
  truths:
    - "ActionLogEntry dataclass stores timestamp, action, player, success, error"
    - "JsonFormatter emits one JSON object per line with UTC timestamp"
    - "RateLimiter enforces configurable delay range with jitter"
    - "ErrorHandler wraps functions with tenacity retry on Playwright errors"
    - "Session expiry detection returns bool from page state check"
    - "UI element not found triggers fallback with page reload"
  artifacts:
    - path: "models/action_log.py"
      provides: "ActionLogEntry dataclass + JsonFormatter class"
      contains: "class ActionLogEntry, class JsonFormatter"
    - path: "browser/rate_limiter.py"
      provides: "RateLimiter class with wait/wait_if_needed"
      contains: "class RateLimiter"
    - path: "browser/error_handler.py"
      provides: "RetryHandler with tenacity decorators + session helpers"
      contains: "class RetryHandler, def ensure_session"
    - path: "tests/test_action_log.py"
      provides: "Tests for ActionLogEntry, JsonFormatter, history parsing"
      min_tests: 6
    - path: "tests/test_rate_limiter.py"
      provides: "Tests for RateLimiter wait, wait_if_needed, config"
      min_tests: 4
    - path: "tests/test_error_handler.py"
      provides: "Tests for retry backoff, session detection, element fallback"
      min_tests: 5
  key_links:
    - from: "models/action_log.py"
      to: "logs/actions.jsonl"
      via: "JsonFormatter output format (JSONL)"
      pattern: "json\\.dumps"
    - from: "browser/error_handler.py"
      to: "playwright.sync_api"
      via: "PlaywrightError, PlaywrightTimeoutError imports"
      pattern: "from playwright\\.sync_api import"
    - from: "browser/rate_limiter.py"
      to: "config/config.py"
      via: "RateLimitingConfig min_delay_ms/max_delay_ms"
      pattern: "RateLimitingConfig|RateLimiter"
---

<objective>
TDD foundation for Phase 5: Create ActionLogEntry model, RateLimiter, and ErrorHandler with failing tests first, then implement to pass.

Purpose: Build the three core modules that all subsequent plans depend on, following project TDD convention (RED → GREEN).
Output: 3 new modules with passing tests, requirements.txt updated with tenacity + rich.
</objective>

<execution_context>
@C:/Users/Ebby/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Ebby/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-logging-error-handling/05-RESEARCH.md

@models/relist_result.py
@config/config.py
@browser/relist.py
@browser/navigator.py
</context>

<interfaces>
From config/config.py:
```python
@dataclass
class RateLimitingConfig:
    min_delay_ms: int = 2000
    max_delay_ms: int = 5000
    def __post_init__(self): # validates min <= max

@dataclass
class AppConfig:
    rate_limiting: RateLimitingConfig = field(default_factory=RateLimitingConfig)
    def to_dict(self) -> dict
    @classmethod
    def from_dict(cls, data: dict) -> AppConfig
```

From models/relist_result.py:
```python
@dataclass
class RelistResult:
    listing_index: int
    player_name: str
    old_price: int | None
    new_price: int | None
    success: bool
    error: str | None = None
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ActionLogEntry dataclass + JsonFormatter + tests</name>
  <files>models/action_log.py, tests/test_action_log.py</files>
  <behavior>
    - ActionLogEntry: dataclass with timestamp (datetime), level (str), action (str), player_name (optional str), success (bool), message (str), error (optional str), extra (optional dict)
    - __post_init__: validate level in {INFO, WARNING, ERROR}, action is non-empty
    - to_dict(): returns flat dict suitable for JSON serialization
    - from_dict(cls, data): reconstruct from parsed JSON
    - JsonFormatter(logging.Formatter): format() returns json.dumps with timestamp (UTC ISO), level, logger, message + any extra fields (action, player_name, success, exception)
    - Test 1: ActionLogEntry creation with required fields
    - Test 2: ActionLogEntry.to_dict() produces correct structure
    - Test 3: ActionLogEntry.from_dict() round-trips correctly
    - Test 4: JsonFormatter produces valid JSON with expected keys
    - Test 5: JsonFormatter includes exception info when present
    - Test 6: JsonFormatter includes extra fields (action, player_name, success)
    - Test 7: parse_action_history() reads JSONL file, returns list of dicts
  </behavior>
  <action>
    Create tests/test_action_log.py FIRST with all 7 test cases (RED). Tests will fail since models/action_log.py doesn't exist yet.

    Then create models/action_log.py:
    - ActionLogEntry dataclass following the same pattern as RelistResult in models/relist_result.py
    - Fields: timestamp (datetime), level (str), action (str), player_name (str|None), success (bool), message (str), error (str|None), extra (dict|None)
    - __post_init__ validates level in {"INFO", "WARNING", "ERROR"} and action non-empty
    - to_dict() returns flat dict, from_dict() class method for round-trip
    - JsonFormatter(logging.Formatter) subclass: format(record) builds dict with timestamp (UTC ISO from record.created), level, logger, message, includes extra fields via hasattr checks, includes exception via formatException if exc_info present
    - Standalone function parse_action_history(path: Path, lines: int = 20) -> list[dict]: reads last N lines of JSONL file, json.loads each

    Run tests (GREEN), verify all 7 pass.
  </action>
  <verify>
    <automated>pytest tests/test_action_log.py -x -v</automated>
  </verify>
  <done>7 tests pass, ActionLogEntry and JsonFormatter are importable and functional</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RateLimiter class + tests</name>
  <files>browser/rate_limiter.py, tests/test_rate_limiter.py</files>
  <behavior>
    - RateLimiter(min_delay_ms, max_delay_ms): stores config, tracks _last_action_time
    - wait(): sleeps random(min, max) ms, updates _last_action_time
    - wait_if_needed(): if elapsed < min_delay, sleeps remaining time only
    - last_delay_ms property: returns the last delay applied (for logging)
    - Test 1: RateLimiter init with defaults from RateLimitingConfig (2000, 5000)
    - Test 2: wait() sleeps at least min_delay and updates _last_action_time
    - Test 3: wait_if_needed() sleeps remaining time if called immediately after wait()
    - Test 4: wait_if_needed() does NOT sleep if enough time has passed
    - Test 5: RateLimiter can be initialized from RateLimitingConfig dataclass
  </behavior>
  <action>
    Create tests/test_rate_limiter.py FIRST with 5 test cases (RED). Use monkeypatch for time.sleep to keep tests fast.

    Then create browser/rate_limiter.py:
    - RateLimiter class following research pattern (RESEARCH.md Pattern 4)
    - __init__(self, min_delay_ms=2000, max_delay_ms=5000)
    - _last_action_time: float = 0
    - _last_delay_ms: int = 0
    - wait() -> None: random delay, sleep, update timestamps
    - wait_if_needed() -> None: check elapsed, sleep remainder if needed
    - Class method from_config(cls, config: RateLimitingConfig) -> RateLimiter
    - Use logging.getLogger(__name__), Italian debug messages ("Rate limit: attesa Xms")

    Run tests (GREEN), verify all 5 pass.
  </action>
  <verify>
    <automated>pytest tests/test_rate_limiter.py -x -v</automated>
  </verify>
  <done>5 tests pass, RateLimiter enforces delays correctly</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ErrorHandler (retry + session helpers) + tests</name>
  <files>browser/error_handler.py, tests/test_error_handler.py</files>
  <behavior>
    - RetryHandler wraps functions with tenacity @retry for Playwright errors
    - retry_on_timeout(func): decorator that retries on PlaywrightError/PlaywrightTimeoutError, exponential backoff 2-30s, max 3 attempts, logs warning before each retry in Italian
    - is_session_expired(page): returns True if page URL contains "login" or no .ut-app element
    - handle_element_not_found(page, selector, fallback_reload=True): tries reload + retry if element missing
    - Test 1: retry_on_timeout retries on PlaywrightError and eventually raises after max attempts
    - Test 2: retry_on_timeout succeeds on second attempt (mock succeeds after 1 fail)
    - Test 3: is_session_expired returns True when URL contains "login"
    - Test 4: is_session_expired returns False when .ut-app element present
    - Test 5: handle_element_not_found reloads page when element not found and fallback_reload=True
  </behavior>
  <action>
    Create tests/test_error_handler.py FIRST with 5 test cases (RED). Use unittest.mock for Playwright page objects.

    Then create browser/error_handler.py:
    - Import tenacity: retry, stop_after_attempt, wait_exponential, retry_if_exception_type
    - Import from playwright.sync_api: Error as PlaywrightError, TimeoutError as PlaywrightTimeoutError
    - retry_on_timeout(func) decorator: @retry with retry_if_exception_type(PlaywrightError | PlaywrightTimeoutError), wait_exponential(multiplier=1, min=2, max=30), stop=stop_after_attempt(3), reraise=True, before_sleep logs Italian warning with attempt number and next sleep time
    - is_session_expired(page: Page) -> bool: checks if "login" in page.url.lower(), or .ut-app selector not found, or .ea-app selector not found. Returns True if session expired.
    - handle_element_not_found(page: Page, selector: str, timeout: int = 5000, fallback_reload: bool = True) -> bool: tries page.query_selector with wait, if fails and fallback_reload, calls page.reload() + page.wait_for_timeout(3000) + retry. Returns True if found, False if not.
    - ensure_session(page: Page, auth: AuthManager, controller: BrowserController) -> bool: calls is_session_expired, if expired: auth.delete_saved_session(), page.reload(), get_credentials + auth.perform_login + auth.save_session. Returns True if session valid/recovered.
    - All logging in Italian per project convention

    Run tests (GREEN), verify all 5 pass.
  </action>
  <verify>
    <automated>pytest tests/test_error_handler.py -x -v</automated>
  </verify>
  <done>5 tests pass, retry and session recovery logic verified</done>
</task>

</tasks>

<verification>
1. All 3 test files exist and pass: `pytest tests/test_action_log.py tests/test_rate_limiter.py tests/test_error_handler.py -v`
2. Full suite (50 existing + new tests) passes: `pytest tests/ -v`
3. requirements.txt includes tenacity>=8.0 and rich>=13.0
4. All imports resolve without errors
</verification>

<success_criteria>
- ActionLogEntry dataclass with to_dict/from_dict round-trip
- JsonFormatter producing valid JSONL output
- RateLimiter enforcing delay range with jitter
- RetryHandler wrapping Playwright calls with exponential backoff
- Session expiry detection from page state
- Element not found fallback with reload
- 17+ new tests all passing
- requirements.txt updated
</success_criteria>

<output>
After completion, create `.planning/phases/05-logging-error-handling/05-00-SUMMARY.md`
</output>
