---
phase: 05-logging-error-handling
plan: 03
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
files_modified:
  - main.py
  - requirements.txt
autonomous: false
requirements:
  - LOG-03

must_haves:
  truths:
    - "Console shows real-time status table during scan/relist cycle (scanned, relisted, errors)"
    - "Status display uses rich Live and doesn't collide with log output"
    - "Rich is installed and in requirements.txt"
    - "Full test suite passes after all changes"
  artifacts:
    - path: "main.py"
      provides: "Rich Live status display during scan cycle"
      contains: "rich.live.Live, make_status_table"
  key_links:
    - from: "main.py main()"
      to: "rich.live.Live"
      via: "Live context manager wrapping scan/relist loop"
      pattern: "Live\\(|rich\\.live"
    - from: "rich.live.Live"
      to: "logging.StreamHandler"
      via: "Console redirect to avoid stdout collision"
      pattern: "console\\.print|rich\\.console"
---

<objective>
Add real-time console status display using rich Live and perform final integration verification.

Purpose: Provide real-time feedback during scan/relist cycles (LOG-03) and verify all Phase 5 components work together.
Output: Rich Live status table in main loop, all Phase 5 requirements satisfied.
</objective>

<execution_context>
@C:/Users/Ebby/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Ebby/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-logging-error-handling/05-RESEARCH.md

@main.py
@browser/relist.py
@browser/navigator.py
</context>

<interfaces>
From browser/relist.py (after Plan 02):
```python
class RelistExecutor:
    def relist_single(self, listing) -> RelistResult
    def relist_expired(self, expired_listings) -> RelistBatchResult
```

From models/relist_result.py:
```python
@dataclass
class RelistBatchResult:
    total: int
    succeeded: int
    failed: int
    results: list[RelistResult]
    success_rate: float  # property
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add rich Live status display to main loop</name>
  <files>main.py, requirements.txt</files>
  <action>
    Add real-time console status using rich.live.Live:

    1. Ensure requirements.txt includes rich>=13.0 (add if not present from Plan 00)
    2. At the top of main.py, import:
       from rich.live import Live
       from rich.table import Table
       from rich.console import Console
    3. Create helper function make_status_table(phase: str, scanned: int, relisted: int, errors: int) -> Table:
       - Table title: "FIFA Auto-Relist"
       - Columns: "Fase" (cyan), "Scansionati" (right), "Rilistati" (right, green), "Errori" (right, red)
       - Add row with the 4 values
       - Return the Table
    4. In main(), wrap the scan/relist section with Live display:
       - Before the scan: Live(make_status_table("In attesa...", 0, 0, 0), refresh_per_second=2, console=Console(stderr=True)) as live:
       - Important: Use Console(stderr=True) so the Live display writes to stderr, avoiding collision with the logging StreamHandler on stdout
       - After navigation: live.update(make_status_table("Navigazione...", 0, 0, 0))
       - After scan: live.update(make_status_table("Scansione", total, 0, 0))
       - During relist: update with running counts
       - After relist: live.update(make_status_table("Completato", total, succeeded, failed))
    5. Keep the existing logger.info() calls — they still go to stdout + app.log + actions.jsonl
    6. The Live display goes to stderr, so it doesn't collide with log output

    Use Italian labels per project convention.
    Note: If rich Live causes issues with the input() prompt at the end, wrap input() outside the Live context or use console.print for the prompt.
  </action>
  <verify>
    <automated>python -c "from rich.live import Live; from rich.table import Table; from rich.console import Console; t = Table(title='Test'); t.add_column('Fase'); t.add_row('OK'); print('Rich import OK')"</automated>
  </verify>
  <done>Rich Live status table displays during scan/relist, no collision with log output, rich in requirements.txt</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — full Phase 5 integration check</name>
  <what-built>Phase 5 complete: structured JSON logging, action history CLI, rate limiting, session recovery, retry with backoff, real-time status display</what-built>
  <how-to-verify>
    1. Run full test suite: `pytest tests/ -v` — all tests should pass
    2. Check action log: `python -c "from models.action_log import JsonFormatter; print('JsonFormatter OK')"`
    3. Check history CLI: `python main.py history` — should show "Nessuna cronologia azioni trovata." or recent entries
    4. Check rate limiter: `python -c "from browser.rate_limiter import RateLimiter; r = RateLimiter(100, 200); r.wait(); print('RateLimiter OK')"`
    5. Check error handler: `python -c "from browser.error_handler import retry_on_timeout, is_session_expired, ensure_session; print('ErrorHandler OK')"`
    6. Verify requirements.txt: should contain tenacity>=8.0 and rich>=13.0
    7. Verify actions.jsonl handler: after a simulated relist, check logs/actions.jsonl has JSON entries
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

</tasks>

<verification>
1. Full test suite passes: pytest tests/ -v (50+ tests)
2. Rich Live displays status table without flickering
3. Console output and log output don't collide
4. History subcommand accessible via CLI
5. All 8 requirements (LOG-01 through LOG-04, ERROR-01 through ERROR-04) satisfied
</verification>

<success_criteria>
- Real-time status table shows scanned/relisted/errors counts
- Console display uses rich Live with stderr redirect
- No collision between Live display and log output
- All Phase 5 requirements verified
- Human verification checkpoint passed
</success_criteria>

<output>
After completion, create `.planning/phases/05-logging-error-handling/05-03-SUMMARY.md`
</output>
