---
phase: 06-telegram-commands
plan: 00
type: tdd
wave: 1
depends_on: []
files_modified:
  - bot_state.py
  - telegram_handler.py
  - browser/sold_handler.py
  - tests/test_bot_state.py
  - tests/test_telegram_handler.py
  - tests/test_sold_handler.py
autonomous: true
requirements:
  - TELEGRAM-01
  - TELEGRAM-02
  - TELEGRAM-03
  - TELEGRAM-04
  - TELEGRAM-05
  - TELEGRAM-06
  - TELEGRAM-07
  - TELEGRAM-08
  - TELEGRAM-09
user_setup: []

must_haves:
  truths:
    - "BotState can be read/written safely from multiple threads"
    - "Telegram handler parses commands and routes to correct handlers"
    - "Sold handler can collect credits and clear sold items"
  artifacts:
    - path: "bot_state.py"
      provides: "Thread-safe BotState dataclass with Lock"
      exports: ["BotState"]
    - path: "telegram_handler.py"
      provides: "TelegramHandler class with command routing"
      exports: ["TelegramHandler"]
    - path: "browser/sold_handler.py"
      provides: "SoldHandler for navigating and clearing sold items"
      exports: ["SoldHandler", "SoldCreditsResult"]
  key_links:
    - from: "telegram_handler.py"
      to: "bot_state.py"
      via: "BotState method calls (set_paused, set_force_relist, etc.)"
      pattern: "self\\.bot_state\\.(set_|get_)"
    - from: "telegram_handler.py"
      to: "notifier.py"
      via: "reuse send_telegram_alert / send_telegram_photo patterns"
      pattern: "urllib\\.request"
    - from: "browser/sold_handler.py"
      to: "browser/detector.py"
      via: "similar DOM scanning pattern for sold items"
      pattern: "eval_on_selector_all|get_by_role"
---

<objective>
TDD: Create BotState, TelegramHandler, and SoldHandler with full test coverage

Purpose: Establish the core modules with test-first approach — BotState for thread-safe shared state, TelegramHandler for command polling and routing, SoldHandler for sold items cleanup.
Output: Three new modules with passing tests, no integration into main.py yet.
</objective>

<execution_context>
@C:/Users/Ebby/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Ebby/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.1-telegram-commands/REQUIREMENTS.md
@.planning/milestones/v1.1-telegram-commands/RESEARCH.md
@notifier.py
@config/config.py
@browser/detector.py
@browser/navigator.py

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From notifier.py (reuse urllib patterns):
```python
def send_telegram_alert(config, message: str) -> None:
    """Invia un messaggio Telegram usando la configurazione NotificationsConfig."""

def send_telegram_photo(config, photo_path: str, caption: str) -> None:
    """Invia una foto a Telegram con didascalia (multipart/form-data)."""
```

From config/config.py (NotificationsConfig):
```python
@dataclass
class NotificationsConfig:
    telegram_token: str = ""
    telegram_chat_id: str = ""
```

From browser/detector.py (DOM scanning pattern to follow):
```python
class ListingDetector:
    def __init__(self, page):
        self.page = page

    def scan_listings(self) -> ListingScanResult:
        """Scan DOM for all listings, return structured result."""
```

From browser/navigator.py (navigation pattern to follow):
```python
class TransferMarketNavigator:
    def __init__(self, page, config):
        self.page = page
        self.config = config

    def go_to_transfer_list(self) -> bool:
        """Navigate to Transfer List page."""
```

From models/listing.py (data model pattern to follow):
```python
@dataclass
class ListingScanResult:
    listings: list[PlayerListing]
    total_count: int
    expired_count: int
    active_count: int
    sold_count: int
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TDD — BotState dataclass with thread safety</name>
  <files>bot_state.py, tests/test_bot_state.py</files>
  <behavior>
    - BotState() initializes with paused=False, force_relist=False, cycle_count=0, last_relisted=0, last_failed=0, last_scan_time=None
    - set_paused(True) → is_paused() returns True
    - set_paused(False) → is_paused() returns False
    - set_force_relist(True) → consume_force_relist() returns True and resets flag to False
    - consume_force_relist() when flag is False → returns False
    - update_stats(cycle=1, relisted=3, failed=1) → updates counters, sets last_scan_time
    - get_status() returns dict with all current state values
    - Thread safety: 100 concurrent threads calling set_paused/get_status don't corrupt state
  </behavior>
  <action>
    RED: Create tests/test_bot_state.py with all behavior tests above. Run pytest — all must fail (file doesn't exist yet).

    GREEN: Create bot_state.py with:
    - BotState dataclass with threading.Lock
    - All methods: __init__, set_paused, is_paused, set_force_relist, consume_force_relist, update_stats, get_status
    - Use self._lock for all read/write operations
    - last_scan_time as datetime | None

    REFACTOR: Ensure clean code, Italian comments where appropriate, follow project patterns.

    Use threading.Lock, not RLock (no nested locking needed).
    Use dataclass with field(default_factory=...) for mutable defaults.
  </action>
  <verify>
    <automated>pytest tests/test_bot_state.py -x -v</automated>
  </verify>
  <done>All BotState tests pass. Thread safety verified with concurrent test. Dataclass exports correctly.</done>
</task>

<task type="auto">
  <name>Task 2: TDD — TelegramHandler command routing and responses</name>
  <files>telegram_handler.py, tests/test_telegram_handler.py</files>
  <behavior>
    - _parse_command("/status") → ("status", [])
    - _parse_command("/logs 10") → ("logs", ["10"])
    - _parse_command("/pause") → ("pause", [])
    - _parse_command("/unknown") → ("unknown", []) — handled as unknown command
    - _handle_command("status") → formatted status message using bot_state.get_status()
    - _handle_command("pause") → bot_state.set_paused(True), returns "⏸️ Bot in pausa"
    - _handle_command("resume") → bot_state.set_paused(False), returns "▶️ Bot riavviato"
    - _handle_command("force_relist") → bot_state.set_force_relist(True), returns "⚡ Force relist attivato"
    - _handle_command("help") → returns formatted list of all commands
    - _handle_command("logs") → returns last 20 lines of logs/app.log (default)
    - _handle_command("logs", ["5"]) → returns last 5 lines
    - _handle_command("unknown") → returns "❌ Comando sconosciuto. Usa /help"
    - _get_updates(offset=0) → mocked urllib call returning update list
    - _send_message(text) → mocked urllib POST to sendMessage
    - Unauthorized chat_id → command ignored, no response sent
  </behavior>
  <action>
    RED: Create tests/test_telegram_handler.py with all behavior tests above. Mock urllib.request.urlopen using unittest.mock.patch. Run pytest — all must fail.

    GREEN: Create telegram_handler.py with:
    - TelegramHandler class: __init__(token, chat_id, bot_state, page=None, log_dir=None)
    - Public: start(), stop(), send_message(text)
    - Private: _poll(), _get_updates(offset), _handle_update(update), _parse_command(text), _handle_command(cmd, args)
    - Command handlers: _cmd_status(), _cmd_pause(), _cmd_resume(), _cmd_force_relist(), _cmd_screenshot(), _cmd_del_sold(), _cmd_logs(), _cmd_help()
    - Use urllib.request (no extra deps) — follow notifier.py patterns
    - Long polling with offset tracking, 30s timeout
    - Thread with threading.Event for clean shutdown
    - Chat ID authorization check before processing commands

    For /screenshot and /del_sold: stub methods that log "not implemented without page" — actual implementation needs Playwright page reference (will be wired in Plan 01).

    For /logs: read last N lines from logs/app.log using Path.read_text().splitlines()[-N:].

    For /help: return formatted message listing all 8 commands with descriptions.

    REFACTOR: Clean up, ensure Italian messages, follow notifier.py urllib patterns.
  </action>
  <verify>
    <automated>pytest tests/test_telegram_handler.py -x -v</automated>
  </verify>
  <done>All TelegramHandler tests pass. Command routing verified. Mocked urllib calls work. Unauthorized chat_id ignored.</done>
</task>

<task type="auto">
  <name>Task 3: TDD — SoldHandler for sold items cleanup</name>
  <files>browser/sold_handler.py, tests/test_sold_handler.py, models/sold_result.py</files>
  <behavior>
    - SoldCreditsResult: dataclass with total_credits (int), items_cleared (int), success (bool), error (str | None)
    - SoldHandler.__init__(page, config) — stores page and config
    - SoldHandler._navigate_to_sold_items() — navigates to Sold Items page (Transfers → Sold Items)
    - SoldHandler._collect_sold_credits() — reads sold item prices from DOM, sums them
    - SoldHandler._clear_sold_items() — clicks "Clear Sold Items" button
    - SoldHandler.process_sold_items() → SoldCreditsResult: navigates, collects, clears, returns result
    - Error handling: if navigation fails → SoldCreditsResult(success=False, error="Navigation failed")
    - Error handling: if no sold items → SoldCreditsResult(total_credits=0, items_cleared=0, success=True)
    - Rate limiting: use RateLimiter between actions (follow existing pattern)
  </behavior>
  <action>
    RED: Create tests/test_sold_handler.py with all behavior tests above. Mock Playwright page object. Run pytest — all must fail.

    Create models/sold_result.py with SoldCreditsResult dataclass.

    GREEN: Create browser/sold_handler.py with:
    - SoldCreditsResult dataclass in models/sold_result.py
    - SoldHandler class: __init__(page, config), process_sold_items() → SoldCreditsResult
    - SELECTORS dict for sold items DOM elements (follow detector.py pattern):
      - "sold_items_tab": selector for Sold Items tab
      - "clear_sold_items": selector for clear button
      - "sold_item_price": selector for price elements
    - _navigate_to_sold_items(): Home → Transfers → Sold Items (follow navigator.go_to_transfer_list pattern)
    - _collect_sold_credits(): scan DOM for sold item prices, parse coin values, sum total
    - _clear_sold_items(): click clear button, confirm if dialog appears
    - Use RateLimiter between actions (2-5s delay)
    - Italian logging throughout

    For DOM selectors, use get_by_role() pattern (per AGENTS.md rule #6). The Sold Items tab is typically under Transfers section. The clear button may have a confirmation dialog.

    REFACTOR: Clean up, ensure Italian comments, follow existing browser/ module patterns.
  </action>
  <verify>
    <automated>pytest tests/test_sold_handler.py -x -v</automated>
  </verify>
  <done>All SoldHandler tests pass. SoldCreditsResult model correct. DOM parsing logic tested with mocked page.</done>
</task>

</tasks>

<verification>
Full test suite passes: pytest tests/test_bot_state.py tests/test_telegram_handler.py tests/test_sold_handler.py -v
All three modules import cleanly: python -c "from bot_state import BotState; from telegram_handler import TelegramHandler; from browser.sold_handler import SoldHandler; from models.sold_result import SoldCreditsResult"
No new dependencies added to requirements.txt
</verification>

<success_criteria>
1. BotState: thread-safe, all methods work, concurrent access safe
2. TelegramHandler: all 8 commands parse and route correctly, mocked urllib works
3. SoldHandler: navigation, credit collection, clearing all tested with mocks
4. All tests pass (new tests + existing 68+ tests)
5. No modifications to golden hour logic, detector, navigator, or relist core
</success_criteria>

<output>
After completion, create `.planning/phases/06-telegram-commands/06-00-SUMMARY.md`
</output>
