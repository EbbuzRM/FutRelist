---
phase: 05-logging-error-handling
plan: 02
type: execute
wave: 2
depends_on:
  - 05-00
files_modified:
  - browser/navigator.py
  - browser/relist.py
  - main.py
autonomous: true
requirements:
  - ERROR-01
  - ERROR-02
  - ERROR-03
  - ERROR-04

must_haves:
  truths:
    - "Rate limiting is centralized via RateLimiter (not scattered _random_delay calls)"
    - "Network errors trigger automatic retry with exponential backoff"
    - "Session expiration is detected at the start of each scan cycle"
    - "UI element not found triggers page reload and retry"
    - "Rate limiter delays are enforced between scan cycles in the main loop"
  artifacts:
    - path: "browser/navigator.py"
      provides: "Updated to use RateLimiter instead of _random_delay"
      contains: "RateLimiter import, self.rate_limiter"
    - path: "browser/relist.py"
      provides: "Updated to use RateLimiter instead of _random_delay"
      contains: "RateLimiter import, self.rate_limiter"
    - path: "main.py"
      provides: "Session check + rate limiting in scan loop + retry wrapping"
      contains: "ensure_session call, rate_limiter.wait, retry_on_timeout"
  key_links:
    - from: "main.py main()"
      to: "browser/rate_limiter.py"
      via: "RateLimiter instantiation from app_config.rate_limiting"
      pattern: "RateLimiter\\.from_config|RateLimiter\\("
    - from: "main.py scan loop"
      to: "browser/error_handler.py"
      via: "ensure_session() call before each scan cycle"
      pattern: "ensure_session"
    - from: "browser/navigator.py"
      to: "browser/rate_limiter.py"
      via: "self.rate_limiter.wait() replaces self._random_delay()"
      pattern: "rate_limiter\\.wait"
    - from: "browser/relist.py"
      to: "browser/rate_limiter.py"
      via: "self.rate_limiter.wait() replaces self._random_delay()"
      pattern: "rate_limiter\\.wait"
---

<objective>
Integrate error recovery and rate limiting into the browser modules and main loop: replace scattered _random_delay with centralized RateLimiter, wrap navigation with retry, add session check at scan cycle start.

Purpose: Make the tool resilient to network errors (ERROR-01), session expiry (ERROR-02), UI changes (ERROR-03), and enforce consistent rate limiting (ERROR-04).
Output: Updated navigator.py, relist.py with RateLimiter, main.py with session check + retry + rate limiting in scan loop.
</objective>

<execution_context>
@C:/Users/Ebby/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/Ebby/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-logging-error-handling/05-RESEARCH.md

@main.py
@browser/navigator.py
@browser/relist.py
@browser/controller.py
@browser/auth.py
@browser/error_handler.py
@browser/rate_limiter.py
@config/config.py
</context>

<interfaces>
From browser/rate_limiter.py (created in Plan 00):
```python
class RateLimiter:
    def __init__(self, min_delay_ms=2000, max_delay_ms=5000)
    def wait(self) -> None
    def wait_if_needed(self) -> None
    @classmethod
    def from_config(cls, config: RateLimitingConfig) -> RateLimiter
```

From browser/error_handler.py (created in Plan 00):
```python
def retry_on_timeout(func)  # tenacity decorator
def is_session_expired(page: Page) -> bool
def handle_element_not_found(page, selector, timeout, fallback_reload) -> bool
def ensure_session(page, auth, controller) -> bool
```

From config/config.py:
```python
@dataclass
class RateLimitingConfig:
    min_delay_ms: int = 2000
    max_delay_ms: int = 5000
```

From browser/auth.py:
```python
class AuthManager:
    def is_logged_in(self, page: Page) -> bool
    def delete_saved_session(self) -> None
    def perform_login(self, page, email, password) -> bool
    def save_session(self, context) -> None
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Replace _random_delay with RateLimiter in navigator.py and relist.py</name>
  <files>browser/navigator.py, browser/relist.py</files>
  <action>
    In browser/navigator.py:
    1. Import RateLimiter from browser.rate_limiter
    2. In __init__, replace the manual min_delay_ms/max_delay_ms extraction with:
       self.rate_limiter = RateLimiter.from_config(config.get("rate_limiting", {})) if isinstance config, else RateLimiter(min_delay_ms, max_delay_ms)
       Actually simpler: create RateLimiter directly from the dict values:
       rate_limiting = config.get("rate_limiting", {})
       self.rate_limiter = RateLimiter(rate_limiting.get("min_delay_ms", 2000), rate_limiting.get("max_delay_ms", 5000))
    3. Replace all self._random_delay() calls with self.rate_limiter.wait()
    4. Remove the _random_delay method entirely
    5. Remove the self.min_delay_ms and self.max_delay_ms attributes

    In browser/relist.py:
    1. Import RateLimiter from browser.rate_limiter
    2. In __init__, replace manual extraction with:
       rate_limiting = config.get("rate_limiting", {})
       self.rate_limiter = RateLimiter(rate_limiting.get("min_delay_ms", 2000), rate_limiting.get("max_delay_ms", 5000))
    3. Replace all self._random_delay() calls with self.rate_limiter.wait()
    4. Remove the _random_delay method entirely
    5. Remove the self.min_delay_ms and self.max_delay_ms attributes

    Keep existing logging messages in Italian.
    Do NOT change the return types or method signatures of go_to_transfer_list, relist_single, relist_expired.
  </action>
  <verify>
    <automated>pytest tests/test_rate_limiter.py tests/test_relist.py -x -v</automated>
  </verify>
  <done>navigator.py and relist.py use centralized RateLimiter, _random_delay removed, existing tests still pass</done>
</task>

<task type="auto">
  <name>Task 2: Wire session check + retry + rate limiting into main loop</name>
  <files>main.py</files>
  <action>
    Update main() in main.py to add error recovery in the scan/relist loop:

    1. Import ensure_session from browser.error_handler
    2. Import retry_on_timeout from browser.error_handler
    3. Import RateLimiter from browser.rate_limiter
    4. In main(), after creating the navigator and detector (around line 115):
       - Create RateLimiter: rate_limiter = RateLimiter(app_config.rate_limiting.min_delay_ms, app_config.rate_limiting.max_delay_ms)
    5. Wrap the scan section (navigator.go_to_transfer_list + detector.scan_listings) in a loop that:
       a. At the start of each iteration: calls ensure_session(page, auth, controller) — if returns False, log error and break
       b. Calls navigator.go_to_transfer_list() — if fails, log warning and continue to next iteration after rate_limiter.wait()
       c. Calls detector.scan_listings()
       d. Process relist as before
       e. At end of iteration: rate_limiter.wait() (delay between scan cycles)
       f. Read app_config.scan_interval_seconds and wait that long before next cycle
    6. Wrap navigator.go_to_transfer_list() call with retry_on_timeout if possible (or handle TimeoutError in try/except with retry logic)
    7. Add a --once flag (or keep current single-run behavior) — for now, keep it single-run but add the session check + rate limiter at the scan points

    Specifically for single-run mode (current behavior):
    - Add ensure_session(page, auth, controller) call BEFORE navigator.go_to_transfer_list()
    - Wrap navigator.go_to_transfer_list() in try/except PlaywrightTimeoutError with page reload + retry once
    - Add rate_limiter.wait() after the relist batch completes (before the input() prompt)

    Use Italian log messages per project convention.
  </action>
  <verify>
    <automated>python -c "from browser.error_handler import ensure_session, retry_on_timeout; from browser.rate_limiter import RateLimiter; print('All imports OK')"</automated>
  </verify>
  <done>main.py includes session check before scan, retry on timeout, rate limiter delay after relist batch</done>
</task>

</tasks>

<verification>
1. navigator.py and relist.py no longer contain _random_delay method
2. RateLimiter is instantiated from config dict in both modules
3. main.py calls ensure_session before navigation
4. main.py applies rate_limiter.wait() after relist batch
5. All existing tests pass: pytest tests/ -v
6. Import check: python -c "from browser.navigator import TransferMarketNavigator; from browser.relist import RelistExecutor; print('OK')"
</verification>

<success_criteria>
- Rate limiting centralized in RateLimiter (no more scattered _random_delay)
- Session expiry detected and recovered before each scan
- Network timeout triggers retry with backoff
- Rate limiter enforces delay between scan cycles
- All ERROR-01 through ERROR-04 requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-logging-error-handling/05-02-SUMMARY.md`
</output>
